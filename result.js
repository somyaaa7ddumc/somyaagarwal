let sname, h, e, m, total, avg, result, grade;
sname = prompt('Enter the Name');
h = parseInt(prompt(`Enter the Marks`));
e = parseInt(prompt(`Enter the Marks`));
m = parseInt(prompt(`Enter the Marks`));
total = h + e + m;
avg = total/3;
if(avg>= 1 && avg <= 100) {
    result = (avg >= 60) ? "Pass" : "Fail";
    grade = (avg >=)
}